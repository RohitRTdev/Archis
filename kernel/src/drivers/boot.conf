# Device stack manifest.
# Each [DeviceStack] block: a match id, then driver names bottom -> top.
# id "Root" means a virtual stack brought up at init with no bus enumeration.

[DeviceStack]
Root
test1

[DeviceStack]
test1-child
test2

[DeviceStack]
Root
i8042
